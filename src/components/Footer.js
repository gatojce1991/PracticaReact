import React from 'react';

         const Footer = (props) => (
             <div>
            <footer className="text-center">{props.copyrigth}</footer>
            </div>
        );

export default Footer;